# RES_MAMMO_CUI_VALS

> Stores the CUI value for a finding mark. The CUI value is a string of coordinates that the draw control interprets to display.

**Primary key:** `FINDING_ID`  
**Columns:** 2

[← index](../index.md)

## Columns

| # | Column | Type | Flags | Description |
|--:|--------|------|-------|-------------|
| 1 | `FINDING_ID` | NUMERIC | PK shared | The unique identifier (.1 item) for the finding record. |
| 2 | `MAMMO_CUI_VAL` | VARCHAR |  | Stores the CUI value for a finding mark. The CUI value is a string of coordinates that the draw control interprets to display. |

_Flags: PK = primary key · org = may contain organization-specific values · discont. = discontinued · FK→ = inferred reference (see below) · shared = generic key, intentionally unresolved._

