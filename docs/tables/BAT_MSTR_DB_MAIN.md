# BAT_MSTR_DB_MAIN

> The table for unique, unchanging properties of the batch master type.

**Primary key:** `BATCH_TYPE_ID`  
**Columns:** 3

[← index](../index.md)

## Columns

| # | Column | Type | Flags | Description |
|--:|--------|------|-------|-------------|
| 1 | `BATCH_TYPE_ID` | VARCHAR | PK | The unique ID of the batch master type. |
| 2 | `BATCH_TYPE_ID_BATCH_TYPE_NAME` | VARCHAR |  | The name of the batch master type. |
| 3 | `BATCH_TYPE_NAME` | VARCHAR |  | The name of the batch master type. |

_Flags: PK = primary key · org = may contain organization-specific values · discont. = discontinued · FK→ = inferred reference (see below) · shared = generic key, intentionally unresolved._

