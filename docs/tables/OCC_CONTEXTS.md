# OCC_CONTEXTS

> Stores the context for the order composer context.

**Primary key:** `RECORD_ID`  
**Columns:** 3

[← index](../index.md)

## Columns

| # | Column | Type | Flags | Description |
|--:|--------|------|-------|-------------|
| 1 | `RECORD_ID` | NUMERIC | PK shared | The ID of the Order Composer Configuration (OCC) record. |
| 2 | `RECORD_ID_RECORD_NAME` | VARCHAR |  | Stores the record name of the order composer configuration. |
| 3 | `RECORD_NAME` | VARCHAR |  | Stores the record name of the order composer configuration. |

_Flags: PK = primary key · org = may contain organization-specific values · discont. = discontinued · FK→ = inferred reference (see below) · shared = generic key, intentionally unresolved._

